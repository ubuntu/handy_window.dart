name: handy_window_example
description: Demonstrates the handy_window plugin.
publish_to: "none"

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  handy_window:
    path: ../
  url_launcher: ^6.3.2
  yaru: ^9.0.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
